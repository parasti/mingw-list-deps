
-------------------------------------------------------------------------------

zlib DLL
http://www.zlib.net/

    mkdir zlib123-dll
    unzip zlib123-dll.zip -d zlib123-dll
    cd zlib123-dll
    sh install-zlib-dll.sh

-------------------------------------------------------------------------------

libiconv
http://www.gnu.org/software/libiconv/

    # There is a circular dependency between gettext and libiconv.  As
    # suggested by the libiconv documentation, we'll have to build
    # libiconv, then build gettext and then rebuild libiconv.

    # First time
    mingw-configure --disable-nls
    mingw-make && mingw-make install

    # Second time
    mingw-make distclean
    mingw-configure && mingw-make && mingw-make install

-------------------------------------------------------------------------------

gettext
http://www.gnu.org/software/gettext/

    # Only build DLLs, no tools nor documentation.
    cd gettext-runtime 

    mingw-configure --disable-java
    mingw-make && mingw-make install

-------------------------------------------------------------------------------

DirectX development headers
http://www.libsdl.org/extras/win32/common/

    # These are not strictly necessary, however, making SDL use
    # DirectSound significantly reduces audio latency which is
    # unreasonably high with the WaveOut default.
    tar -xzf directx-devel.tar.gz -C "$MINGW_PREFIX"

-------------------------------------------------------------------------------

SDL
http://www.libsdl.org/

     mingw-configure && mingw-make && mingw-make install

-------------------------------------------------------------------------------

libpng
http://www.libpng.org/pub/png/libpng.html

    mingw-configure && mingw-make && mingw-make install-strip

-------------------------------------------------------------------------------

libjpeg
http://www.ijg.org/

    mingw-configure && mingw-make && mingw-make install-strip

-------------------------------------------------------------------------------

libogg
http://xiph.org/downloads/

    mingw-configure && mingw-make && mingw-make install-strip

-------------------------------------------------------------------------------

libvorbis
http://xiph.org/downloads/

    mingw-configure && mingw-make && mingw-make install-strip

-------------------------------------------------------------------------------

FreeType
http://www.freetype.org/

    mingw-configure && mingw-make && mingw-make install-strip

-------------------------------------------------------------------------------

SDL_ttf
http://www.libsdl.org/projects/SDL_ttf/

    mingw-configure && mingw-make && mingw-make install

-------------------------------------------------------------------------------

PhysicsFS
http://icculus.org/physfs/

    mkdir build && cd build
    mingw-cmake .. && mingw-make && mingw-make install

-------------------------------------------------------------------------------

OpenGL headers
http://opengl.org/registry/

    mkdir -p $MINGW_PREFIX/include/GL
    cp glext.h $MINGW_PREFIX/include/GL/

-------------------------------------------------------------------------------
